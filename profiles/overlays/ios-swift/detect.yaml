name: ios-swift
detect:
  any_file:
    - "*.xcodeproj"
    - "*.xcworkspace"
    - "Package.swift"
    - "project.yml"       # xcodegen
    - "Podfile"           # legacy but common
  any_regex:
    - path: "Package.swift"
      match: "swift-tools-version"
  confidence: high
